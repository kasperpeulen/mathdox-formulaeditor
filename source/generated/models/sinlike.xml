<model:model xmlns:model="local:model">
$main(function(){

  /**
   * Define a semantic tree node that represents the greatest common divisor
   * function.
   */
  org.mathdox.formulaeditor.semantics.<model:cname/> =
    $extend(org.mathdox.formulaeditor.semantics.MultaryOperation, {

      symbol : {

        onscreen : ["<model:name/>(", ")"],
	openmath : "&lt;OMS cd='<model:cd/>' name='<model:name/>'/&gt;"

      },

      precedence : 0

    })

  /**
   * Extend the OpenMathParser object with parsing code for arith1.<model:name/>.
   */
  org.mathdox.formulaeditor.parsing.openmath.OpenMathParser =
    $extend(org.mathdox.formulaeditor.parsing.openmath.OpenMathParser, {

      /**
       * Returns a <model:cname/> object based on the OpenMath node.
       */
      handleArith1<model:cname/> : function(node) {

        // parse the children of the OMA
        var children = node.getChildNodes();
        var operands = new Array(children.getLength()-1);
        for (var i=1; i&lt;children.length; i++) {
          operands[i-1] = this.handle(children.item(i))
        }

        // construct a <model:cname/> object
        var result = new org.mathdox.formulaeditor.semantics.<model:cname/>();
        result.initialize.apply(result, operands);
        return result;

      }

    })

  /**
   * Extend the ExpressionParser object with parsing code for <model:name/> operations.
   */
  with( org.mathdox.formulaeditor.semantics          ) {
  with( org.mathdox.formulaeditor.parsing.expression ) {
  with( new org.mathdox.parsing.ParserGenerator()    ) {

    org.mathdox.formulaeditor.parsing.expression.ExpressionParser =
      $extend(org.mathdox.formulaeditor.parsing.expression.ExpressionParser, {

        // expression6 = <model:name/> | super.expression6
        expression6 : function() {
          var parent = arguments.callee.parent;
          alternation(
            rule("<model:name/>"),
            parent.expression6
          ).apply(this, arguments);
        },

        // <model:name/> = "<model:name/>(" expression ("," expression)* ")"
        <model:name/> :
          transform(
            concatenation(
              literal("<model:name/>("),
              rule("expression"),
              repetition(
                concatenation(
                  literal(","),
                  rule("expression")
                )
              ),
              literal(")")
            ),
            function(result) {
              var array = new Array();
              for (var i=1; i+1&lt;result.length; i=i+2) {
                array.push(result[i]);
              }
              var <model:name/> = new <model:cname/>();
              <model:name/>.initialize.apply(<model:name/>, array);
              return <model:name/>;
            }
          )

      })

  }}}


})
</model:model>
